# Some files and folders are for auto-generated code
# (e.g. /docs)  and others I am just lazy to convert
# to json or similar (e.g. ganalytics_metrics)

exclude_patterns:
  - "**/docs/"
  - "**/ganalytics_metrics.py"
  - "**/templates/"
  - "**/tests/"
